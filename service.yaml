apiVersion: v1
kind: Service
metadata:
  name: hangmanapp-service
spec:
  selector:
    app: hangmanapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30188
  type: NodePort